k2:
  metadata:
    id: k2.templates.code.api.api-gw-lambda
    kind: "template"
  body:
    name: "api-gw-lambda"
    parameters:
      description: "API Web Presence"
      group: "app"
      component: "back"
      routes:
        - public: true
          path: /health
          method: GET
          include: Heath
